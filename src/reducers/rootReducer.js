import { combineReducers } from 'redux'
import ageVerification from './ageVerification'

/**
 * Combines all reducers for simplified imports.
 */
export default combineReducers({
    ageVerification
})